class car:
    def __init__(self, window, door, enginetype):
        self.windows = window
        self.doors = door
        self.engintypes = enginetype

car1 = car(2, 3, "petrol")

print(car1.windows)
print(car1.doors) 
print(car1.engintypes)
# so it is better that we are in 
    